extends layout

block content
  h1= title
  p Welcome to #{title}

  a.btn(href='/glogin') Login via google
  br
  a.btn(href=`https://github.com/login/oauth/authorize?client_id=${clientID}`) Login via GitHub
